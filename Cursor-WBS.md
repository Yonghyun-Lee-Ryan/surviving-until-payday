# 「월급날까지 살아남기」 Cursor 개발 단위

## 0. 공통 개발 원칙

### 기술 기준

* Unity 2D 6000.5.4f1 버전
* C#
* 세로 화면 1080×1920 기준
* Android 우선
* 로컬 싱글플레이
* 데이터와 로직 분리
* UI가 게임 상태를 직접 수정하지 않도록 설계
* 광고는 인터페이스로 추상화
* 사건 데이터는 ScriptableObject 또는 JSON으로 관리
* 모든 핵심 로직은 Unity Scene과 분리해 테스트 가능하게 작성

### 권장 디렉터리

```text
Assets/
├── Art/
├── Audio/
├── Data/
│   ├── Events/
│   ├── Jobs/
│   ├── Traits/
│   └── Endings/
├── Prefabs/
├── Scenes/
│   ├── Bootstrap
│   ├── MainMenu
│   ├── Game
│   └── Result
├── Scripts/
│   ├── Core/
│   ├── Data/
│   ├── Events/
│   ├── UI/
│   ├── Save/
│   ├── Ads/
│   ├── Analytics/
│   └── Debug/
└── Tests/
```

### 핵심 클래스

```text
GameState
RunManager
DayManager
EventManager
EventSelector
EffectResolver
EndingEvaluator
MetaProgressionManager
SaveService
AdService
AnalyticsService
RandomService
```

---

# 개발 단위 1. Unity 프로젝트 기반 구축

## 목표

프로젝트 폴더, Scene, 공통 UI 해상도, 게임 시작 구조를 만든다.

## 구현 내용

* Unity 2D 프로젝트 생성
* Portrait 화면 고정
* Bootstrap Scene 생성
* MainMenu Scene 생성
* Game Scene 생성
* Result Scene 생성
* Scene 전환 관리자 작성
* Safe Area 대응
* 공통 Canvas 설정
* 기본 폴더 구조 생성

## 완료 기준

* 앱 실행 시 Bootstrap에서 MainMenu로 이동한다.
* 게임 시작 버튼을 누르면 Game Scene으로 이동한다.
* 임시 종료 버튼을 누르면 Result Scene으로 이동한다.
* Android 세로 화면에서 UI가 화면 밖으로 벗어나지 않는다.

## Cursor 요청 프롬프트

```text
Unity 2D 모바일 게임 프로젝트의 기반 구조를 구현해줘.

요구사항:
1. Android Portrait 전용 게임이다.
2. Bootstrap, MainMenu, Game, Result Scene을 사용한다.
3. Bootstrap Scene에서 필수 Manager를 초기화하고 MainMenu로 이동한다.
4. Scene 전환을 담당하는 SceneLoader 클래스를 작성한다.
5. DontDestroyOnLoad를 남용하지 말고 AppRoot 하나 아래에서 관리한다.
6. 모든 C# 파일은 Assets/Scripts 아래 역할별 폴더에 생성한다.
7. Canvas는 1080x1920 기준 Scale With Screen Size를 사용한다.
8. Safe Area 대응 컴포넌트를 작성한다.
9. 구현 후 필요한 Unity Editor 설정 절차도 설명한다.

완료 기준을 충족하도록 코드와 작업 순서를 제공해줘.
```

---

# 개발 단위 2. 게임 데이터 모델

## 목표

플레이어 상태, 직업, 특성, 사건, 선택지, 효과 데이터를 정의한다.

## 구현 내용

* GameState
* PlayerStats
* JobData
* TraitData
* EventData
* EventChoiceData
* StatEffect
* EndingData
* Enum 정의
* 데이터 유효성 검사

## 권장 Enum

```csharp
public enum StatType
{
    Cash,
    Health,
    Stress,
    Happiness,
    CompanyScore
}

public enum EventCategory
{
    FixedExpense,
    Work,
    Health,
    Consumption,
    Relationship,
    Opportunity,
    Accident,
    Rest,
    Special
}
```

## 완료 기준

* 하나의 EventData에 제목, 설명, 조건, 선택지 3개를 저장할 수 있다.
* 선택지에 능력치 효과와 확률형 결과를 정의할 수 있다.
* 잘못된 데이터가 있을 때 Console 경고가 출력된다.
* 테스트용 사건 데이터를 코드 또는 ScriptableObject로 생성할 수 있다.

## Cursor 요청 프롬프트

```text
월급날까지 살아남기 게임의 데이터 모델을 구현해줘.

필요한 데이터:
- GameState
- PlayerStats
- JobData
- TraitData
- EventData
- EventChoiceData
- StatEffect
- RandomOutcome
- EventCondition
- EndingData

조건:
1. 현금은 int 또는 long으로 관리한다.
2. 건강, 스트레스, 행복도, 회사 평가는 0~100 범위이다.
3. 사건에는 minDay, maxDay, weight, category가 있다.
4. 사건에는 기본적으로 3개의 선택지가 있다.
5. 선택지는 고정 효과와 확률형 결과를 모두 지원한다.
6. ScriptableObject 기반으로 작성한다.
7. 데이터 유효성 검증을 위한 OnValidate를 작성한다.
8. 런타임 상태와 원본 ScriptableObject 데이터가 섞이지 않게 한다.
9. 클래스별 책임을 명확히 분리한다.
10. 샘플 EventData 생성 예시를 제공한다.
```

---

# 개발 단위 3. 게임 상태 및 능력치 처리

## 목표

선택 결과가 플레이어 상태에 안전하게 반영되도록 한다.

## 구현 내용

* GameState 초기화
* 능력치 변경
* 범위 제한
* 현금 증감
* 상태 변경 이벤트
* 실패 조건 검사
* 상태 스냅샷

## 완료 기준

* 능력치 변경 시 UI에 알림을 보낼 수 있다.
* 건강과 행복도 등이 0~100 범위를 벗어나지 않는다.
* 스트레스 100, 건강 0, 회사 평가 0, 현금 0 미만을 감지한다.
* 상태 변경 전후 값을 기록할 수 있다.

## Cursor 요청 프롬프트

```text
월급날까지 살아남기 게임의 GameState와 능력치 처리 시스템을 구현해줘.

요구사항:
1. 현금, 건강, 스트레스, 행복도, 회사 평가를 관리한다.
2. ApplyEffect 메서드로 하나 이상의 StatEffect를 적용한다.
3. 각 능력치 변경 전후 값을 StatChangeResult로 반환한다.
4. Health, Stress, Happiness, CompanyScore는 0~100으로 Clamp한다.
5. 상태 변경 시 C# event를 발행한다.
6. 파산, 번아웃, 입원, 해고 상태를 판정하는 FailureReason Enum을 만든다.
7. UI 코드를 GameState 내부에 넣지 않는다.
8. NUnit 기반 EditMode 테스트를 작성한다.
9. 경계값과 여러 효과 동시 적용 테스트를 포함한다.
```

---

# 개발 단위 4. 날짜 및 회차 진행

## 목표

1일부터 30일까지 게임이 순서대로 진행되도록 한다.

## 구현 내용

* RunManager
* DayManager
* 시작 상태 설정
* 다음 날 이동
* 주말 판정
* 주간 결산
* 30일 종료
* 날짜별 난도 보정

## 완료 기준

* 게임 시작 시 1일로 초기화된다.
* 사건 선택을 완료하면 다음 날로 이동한다.
* 요일이 정상적으로 계산된다.
* 7, 14, 21일에 주간 결산 이벤트를 발생시킨다.
* 30일 완료 시 성공 상태로 Result Scene으로 이동한다.

## Cursor 요청 프롬프트

```text
30일 생존형 게임의 RunManager와 DayManager를 구현해줘.

요구사항:
1. 게임은 Day 1부터 Day 30까지 진행된다.
2. 날짜마다 요일을 계산한다.
3. 토요일과 일요일을 판정할 수 있어야 한다.
4. 7일, 14일, 21일 종료 시 WeeklySummary 이벤트를 발생시킨다.
5. Day 30의 사건을 해결하면 성공 처리한다.
6. 다음 날로 넘어가기 전에 실패 조건을 검사한다.
7. 날짜 구간에 따른 난도 계수를 반환한다.
8. 게임 로직은 Scene 전환 코드와 분리한다.
9. EditMode 테스트를 작성한다.
```

---

# 개발 단위 5. 사건 선택 엔진

## 목표

현재 날짜와 플레이어 상태에 맞는 사건을 추출한다.

## 구현 내용

* EventSelector
* 날짜 조건
* 능력치 조건
* 직업 조건
* 주말 조건
* 중복 사건 방지
* 가중치 랜덤
* 고정 사건 우선 처리
* 랜덤 시드

## 완료 기준

* 조건에 맞지 않는 사건은 선택되지 않는다.
* 같은 사건이 연속으로 나오지 않는다.
* 고정 지출 사건은 지정된 날짜에 우선 등장한다.
* 동일한 시드에서는 동일한 사건 순서가 나온다.
* 후보 사건이 없을 때 기본 휴식 사건을 반환한다.

## Cursor 요청 프롬프트

```text
게임의 EventSelector를 구현해줘.

요구사항:
1. EventData 목록에서 현재 날짜와 GameState 조건에 맞는 사건을 필터링한다.
2. minDay, maxDay, weekday, weekend, 직업, 능력치 조건을 지원한다.
3. weight를 이용한 가중치 랜덤 선택을 구현한다.
4. 지정 날짜의 Fixed Event는 무작위 사건보다 우선한다.
5. 최근 5개 사건은 반복 선택 확률을 낮춘다.
6. 동일한 Random Seed를 사용하면 같은 사건 순서가 나오게 한다.
7. 선택 가능한 사건이 없으면 fallback 사건을 반환한다.
8. EventSelector는 UnityEngine.Random에 직접 의존하지 않게 한다.
9. 가중치 선택과 조건 필터링에 대한 테스트를 작성한다.
```

---

# 개발 단위 6. 선택지 및 결과 처리

## 목표

선택지를 누르면 고정 효과와 확률형 결과가 처리되도록 한다.

## 구현 내용

* EffectResolver
* 선택지 적용
* 확률형 결과
* 결과 메시지 생성
* 사건 이력 저장
* 선택 잠금
* 다음 날 이동 연계

## 완료 기준

* 선택지를 한 번만 누를 수 있다.
* 결과가 표시되기 전 다음 선택을 할 수 없다.
* 확률형 결과가 시드 기반으로 재현된다.
* 사건 ID, 선택지, 결과, 변경 능력치를 기록한다.

## Cursor 요청 프롬프트

```text
EventChoice 선택과 결과를 처리하는 EffectResolver를 구현해줘.

요구사항:
1. 선택지의 고정 StatEffect를 GameState에 적용한다.
2. RandomOutcome 목록 중 확률에 따라 하나를 선택한다.
3. 동일 시드에서 같은 결과가 나오도록 RandomService를 사용한다.
4. 선택 결과를 ChoiceResult 객체로 반환한다.
5. ChoiceResult에는 사건 ID, 선택지 Index, 메시지, 능력치 변경 전후 값이 포함된다.
6. 사건 기록을 RunHistory에 저장한다.
7. 선택 중복 입력을 방지한다.
8. 결과 처리가 끝나면 DayManager에 다음 날 진행 가능 상태를 전달한다.
9. 핵심 로직에 대한 EditMode 테스트를 작성한다.
```

---

# 개발 단위 7. 게임 플레이 UI

## 목표

플레이어가 사건을 읽고 선택하며 능력치 변화를 확인할 수 있도록 한다.

## 구현 내용

* 날짜 표시
* 현금 표시
* 능력치 게이지
* 사건 카드
* 선택지 버튼 3개
* 결과 팝업
* 수치 변화 애니메이션
* 다음 날 버튼
* 위기 상태 표시

## 완료 기준

* 사건 제목, 설명, 선택지가 표시된다.
* 선택 후 결과와 능력치 변경량이 표시된다.
* 스트레스가 높으면 게이지가 경고 상태로 표시된다.
* 현금 변화가 원 단위 형식으로 표시된다.
* 선택 버튼 연속 입력이 방지된다.

## Cursor 요청 프롬프트

```text
Unity UGUI를 사용해 월급날까지 살아남기 Game Scene UI를 구현해줘.

UI 구성:
- 상단 날짜와 요일
- 보유 현금
- 건강, 스트레스, 행복도, 회사 평가 게이지
- 중앙 사건 제목, 설명, 이미지 영역
- 하단 선택지 버튼 3개
- 선택 결과 팝업
- 다음 날 버튼

요구사항:
1. UI는 GameState를 직접 변경하지 않는다.
2. Presenter 또는 Controller가 GameState 이벤트를 구독해 UI를 갱신한다.
3. 금액은 한국 원화 천 단위 구분 형식으로 표시한다.
4. 능력치 증가와 감소를 간단한 Tween 없이 Coroutine으로 표현한다.
5. 스트레스 80 이상, 건강 20 이하 등의 위기 상태를 시각적으로 표시한다.
6. 선택 버튼 중복 입력을 막는다.
7. 실제 이미지가 없어도 Placeholder로 동작하게 한다.
8. 필요한 Prefab 구조와 Inspector 연결 절차를 설명한다.
```

---

# 개발 단위 8. 실패·성공·엔딩 시스템

## 목표

회차 종료 조건을 판정하고 엔딩을 선택한다.

## 구현 내용

* FailureEvaluator
* EndingEvaluator
* 실패 우선순위
* 성공 엔딩 판정
* ResultData
* 결과 Scene 표시
* 엔딩 도감 등록

## 완료 기준

* 네 가지 실패 조건이 정상 동작한다.
* 30일 생존 시 능력치 기반 엔딩이 선택된다.
* 동시에 여러 조건을 만족해도 우선순위에 따라 하나의 엔딩이 선택된다.
* 엔딩이 도감에 저장된다.

## Cursor 요청 프롬프트

```text
게임의 실패 조건과 엔딩 판정 시스템을 구현해줘.

실패 조건:
- 현금 0 미만: 파산
- 건강 0: 입원
- 스트레스 100: 번아웃
- 회사 평가 0: 해고

성공 조건:
- Day 30까지 생존

요구사항:
1. EndingData를 ScriptableObject로 관리한다.
2. EndingData에는 ID, 제목, 설명, 우선순위, 조건이 포함된다.
3. EndingEvaluator가 현재 GameState에 맞는 가장 우선순위가 높은 엔딩을 반환한다.
4. ResultData에 생존 일수, 최종 능력치, 엔딩, 획득 경험치를 저장한다.
5. Result Scene에서 결과를 표시한다.
6. 새 엔딩이면 도감 해금 처리를 한다.
7. 판정 로직에 대한 테스트를 작성한다.
```

---

# 개발 단위 9. 저장 및 이어하기

## 목표

앱 종료 후에도 현재 회차와 메타 진행 상황을 유지한다.

## 구현 내용

* SaveData
* RunSaveData
* MetaSaveData
* JSON 직렬화
* 자동 저장
* 이어하기
* 저장 버전
* 손상 데이터 처리

## 완료 기준

* 하루가 끝날 때 자동 저장된다.
* 앱을 재실행하면 이어하기가 가능하다.
* 회차 종료 후 현재 회차 저장이 삭제된다.
* 이전 버전 저장 데이터가 있어도 앱이 종료되지 않는다.
* 저장 데이터가 손상되면 초기 상태로 복구한다.

## Cursor 요청 프롬프트

```text
월급날까지 살아남기 게임의 로컬 저장 시스템을 구현해줘.

요구사항:
1. JSON 파일 기반으로 저장한다.
2. RunSaveData와 MetaSaveData를 분리한다.
3. 저장 데이터에는 version 필드를 포함한다.
4. 하루 종료, 회차 종료, 앱 일시정지 시 저장한다.
5. 현재 회차가 있으면 MainMenu에 이어하기 버튼을 표시한다.
6. 회차 종료 시 RunSaveData만 삭제한다.
7. 파일 손상 또는 필드 누락 시 안전하게 기본값을 사용한다.
8. 저장 파일 접근은 ISaveService 인터페이스로 추상화한다.
9. 테스트 가능한 구조로 작성한다.
10. 민감 정보나 실제 결제 정보를 저장하지 않는다.
```

---

# 개발 단위 10. 메타 성장과 도감

## 목표

반복 플레이 이유를 만드는 영구 성장 시스템을 구현한다.

## 구현 내용

* 인생 경험치
* 플레이어 레벨
* 특성 해금
* 사건 도감
* 엔딩 도감
* 업적
* 해금 알림

## 완료 기준

* 회차 결과에 따라 경험치를 획득한다.
* 특정 레벨에서 특성이 해금된다.
* 처음 본 사건과 엔딩이 기록된다.
* 메인 메뉴에서 도감을 확인할 수 있다.
* 같은 해금 보상이 중복 지급되지 않는다.

## Cursor 요청 프롬프트

```text
게임의 MetaProgression 시스템을 구현해줘.

기능:
- 인생 경험치
- 플레이어 레벨
- 특성 해금
- 사건 도감
- 엔딩 도감
- 업적

요구사항:
1. MetaSaveData에 영구 저장한다.
2. 회차 결과에 따라 경험치를 계산한다.
3. 생존 일수, 남은 현금, 새 엔딩 발견에 보너스를 준다.
4. TraitData에 unlockLevel을 정의한다.
5. 새 사건이나 엔딩을 발견하면 도감에 등록한다.
6. 중복 등록을 방지한다.
7. 메인 메뉴에서 해금률을 확인할 수 있다.
8. 해금 시 알림 이벤트를 발생시킨다.
```

---

# 개발 단위 11. 디버그 및 밸런스 도구

## 목표

개인 개발자가 사건과 수치를 빠르게 검증할 수 있도록 한다.

## 구현 내용

* 날짜 이동
* 능력치 수정
* 사건 강제 실행
* 랜덤 시드 고정
* 특정 엔딩 테스트
* 전체 사건 목록
* 30일 자동 시뮬레이션
* 결과 로그 출력

## 완료 기준

* 원하는 날짜와 사건으로 즉시 이동할 수 있다.
* 모든 엔딩을 수동 테스트할 수 있다.
* 1,000회 자동 플레이 결과를 출력할 수 있다.
* 평균 생존 일수와 실패 원인 비율을 확인할 수 있다.
* Release 빌드에서는 디버그 메뉴가 비활성화된다.

## Cursor 요청 프롬프트

```text
게임 밸런스 검증용 DebugPanel과 자동 시뮬레이터를 구현해줘.

DebugPanel 기능:
- 현재 날짜 변경
- 현금과 능력치 변경
- 특정 사건 강제 실행
- Random Seed 설정
- 특정 엔딩 강제 실행
- 게임 즉시 성공 또는 실패

자동 시뮬레이터:
1. 지정한 횟수만큼 30일 회차를 자동 실행한다.
2. 선택지는 랜덤 또는 정책 기반으로 선택한다.
3. 평균 생존 일수, 성공률, 실패 원인 비율, 평균 잔액을 출력한다.
4. Editor에서만 실행 가능하게 한다.
5. Release Build에는 포함하지 않는다.
```

---

# 개발 단위 12. 광고 시스템 추상화

## 목표

실제 광고 SDK를 넣기 전에 게임 로직과 광고를 분리한다.

## 구현 내용

* IAdService
* MockAdService
* RewardedAdPlacement
* 광고 횟수 제한
* 광고 보상 지급
* 광고 실패 처리
* 쿨다운

## 완료 기준

* 에디터에서는 Mock 광고가 즉시 성공한다.
* 광고 실패 시 보상이 지급되지 않는다.
* 광고 완료 시에만 보상이 지급된다.
* 같은 광고 보상을 반복 수령할 수 없다.
* 게임 로직이 특정 광고 SDK를 직접 호출하지 않는다.

## Cursor 요청 프롬프트

```text
광고 SDK에 독립적인 광고 시스템을 구현해줘.

요구사항:
1. IAdService 인터페이스를 만든다.
2. ShowRewardedAd, ShowInterstitial, IsRewardedReady를 제공한다.
3. Editor용 MockAdService를 구현한다.
4. RewardedAdPlacement Enum을 만든다.
5. 광고 완료 Callback 이후에만 보상을 지급한다.
6. 회차당 리롤 2회, 부활 1회 등의 제한을 관리한다.
7. 광고 로딩 실패 시 게임 진행을 막지 않는다.
8. 실제 AdMob SDK는 아직 연결하지 않는다.
9. 추후 SDK 구현체를 교체할 수 있도록 의존성 주입 구조를 사용한다.
```

---

# 개발 단위 13. 분석 이벤트

## 목표

사용자가 어디에서 이탈하고 어떤 선택을 하는지 측정한다.

## 구현 내용

* IAnalyticsService
* DebugAnalyticsService
* 사건 노출
* 선택
* 실패
* 완료
* 광고 제안
* 광고 완료
* 세션 시간

## 완료 기준

* Editor Console에서 분석 이벤트를 확인할 수 있다.
* 사건 ID와 선택지 Index가 기록된다.
* 실패 원인과 생존 날짜가 기록된다.
* 광고 제안 대비 완료 여부가 기록된다.
* 분석 코드가 게임 로직에 흩어지지 않는다.

## Cursor 요청 프롬프트

```text
게임 분석 이벤트 시스템을 구현해줘.

요구사항:
1. IAnalyticsService 인터페이스를 만든다.
2. Editor용 DebugAnalyticsService를 구현한다.
3. 이벤트 이름은 snake_case를 사용한다.
4. run_started, event_shown, choice_selected, run_failed, run_completed,
   rewarded_ad_offered, rewarded_ad_completed 이벤트를 지원한다.
5. 파라미터는 Dictionary<string, object> 형태로 전달한다.
6. 게임 로직은 구체적인 Firebase SDK를 알지 못하게 한다.
7. 이후 FirebaseAnalyticsService를 추가할 수 있게 설계한다.
8. 개인 식별 정보는 기록하지 않는다.
```

---

# 개발 단위 14. 실제 광고 및 Firebase 연결

## 목표

MVP 플레이가 검증된 후 실제 수익화와 분석 SDK를 적용한다.

## 구현 내용

* 모바일 광고 SDK 연결
* 보상형 광고
* 전면 광고
* 실제 분석 이벤트
* Crash 수집
* Remote Config
* 광고 빈도 원격 설정

## 시작 조건

다음 조건을 충족한 뒤 진행한다.

* 광고 없이 게임이 정상적으로 재미있다.
* 20개 사건이 모두 정상 동작한다.
* 저장과 이어하기가 안정적이다.
* 최소 20명의 테스트 플레이 결과를 확보했다.
* 첫 회차 7일 도달률이 70% 이상이다.

## 완료 기준

* 테스트 광고가 정상적으로 출력된다.
* 광고 완료 시 정확히 한 번 보상을 지급한다.
* 광고가 없어도 게임 진행이 가능하다.
* 원격 설정으로 전면 광고 빈도를 변경할 수 있다.
* 오류와 크래시가 수집된다.

---

# 개발 단위 15. 출시 준비

## 목표

Google Play 내부 테스트가 가능한 Android App Bundle을 만든다.

## 구현 내용

* 앱 아이콘
* 시작 화면
* 개인정보처리방침
* 광고 동의 흐름
* 사운드 설정
* 진동 설정
* 저장 데이터 초기화
* 버전 관리
* Release Build
* 내부 테스트 배포

## 완료 기준

* Android App Bundle이 정상 생성된다.
* 실제 기기에서 30일 회차를 완료할 수 있다.
* 앱 강제 종료 후 이어하기가 가능하다.
* 오프라인 환경에서도 핵심 게임을 플레이할 수 있다.
* 광고 로딩 실패 시 오류가 발생하지 않는다.
* 개인정보처리방침 화면으로 이동할 수 있다.

---

# 권장 개발 순서

```text
1. 프로젝트 기반
2. 데이터 모델
3. 게임 상태
4. 날짜 진행
5. 사건 선택
6. 선택 결과
7. 플레이 UI
8. 엔딩
9. 저장
10. 메타 성장
11. 디버그 도구
```

여기까지 완료하면 **광고 없이 플레이 가능한 MVP**가 된다.

그다음 순서는 다음과 같다.

```text
12. 광고 추상화
13. 분석 이벤트
14. 실제 광고·Firebase
15. 출시 준비
```

---

# Cursor 작업 규칙

각 개발 단위를 요청할 때 다음 조건을 공통으로 추가한다.

```text
공통 규칙:
1. 기존 코드를 먼저 분석하고 중복 클래스를 만들지 마라.
2. 한 클래스가 여러 책임을 갖지 않게 하라.
3. UI와 게임 로직을 분리하라.
4. public field 대신 SerializeField private를 우선 사용하라.
5. null 가능성을 검사하고 오류 메시지를 명확하게 작성하라.
6. 수정한 파일 목록을 마지막에 정리하라.
7. Unity Editor에서 필요한 수동 작업을 단계별로 설명하라.
8. 구현 완료 후 테스트 방법을 제공하라.
9. 기존 기능을 파괴할 가능성이 있으면 먼저 영향 범위를 설명하라.
10. 한 번에 다음 개발 단위까지 구현하지 마라.
```

---

# 개발 완료 체크포인트

## MVP 1차

* 사건 하나가 등장한다.
* 선택지를 누르면 능력치가 변경된다.
* 날짜가 넘어간다.

개발 단위 1~7 완료 시점이다.

## MVP 2차

* 30일 전체 진행
* 실패와 성공
* 엔딩
* 저장과 이어하기

개발 단위 8~9 완료 시점이다.

## 알파 테스트

* 메타 성장
* 사건 20개
* 디버그 시뮬레이션
* 밸런스 수정

개발 단위 10~11 완료 시점이다.

## 수익화 테스트

* 보상형 광고
* 전면 광고
* 분석 이벤트

개발 단위 12~14 완료 시점이다.

## 출시 후보

* 앱 번들
* 개인정보처리방침
* 내부 테스트
* 실제 기기 검증

개발 단위 15 완료 시점이다.
