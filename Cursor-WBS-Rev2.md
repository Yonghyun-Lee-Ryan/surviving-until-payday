# 「월급날까지 살아남기」 Cursor 개발 단위 Rev.2

> 기준 문서: `README.md`(기획서), `Cursor-WBS.md`(1차 WBS)  
> 작성 시점: Unit 1~9 완료 이후  
> 목적: 남은 MVP 시스템 단위 + 콘텐츠·고도화 단위를 Cursor에 지시하기 위한 후속 작업 정의

---

## 0. 현재 상태 요약

### 완료된 단위 (Cursor-WBS.md 기준)

| 단위 | 내용 | 상태 |
| --- | --- | --- |
| 1 | Unity 프로젝트 기반 | 완료 |
| 2 | 게임 데이터 모델 | 완료 |
| 3 | 게임 상태·능력치 | 완료 |
| 4 | 날짜·회차 진행 | 완료 |
| 5 | 사건 선택 엔진 | 완료 |
| 6 | 선택지·결과 처리 | 완료 |
| 7 | 게임 플레이 UI | 완료 |
| 8 | 실패·성공·엔딩 | 완료 |
| 9 | 저장·이어하기 | 완료 |

→ **MVP 2차(30일 진행 + 실패/성공 + 엔딩 + 저장)** 까지 도달한 상태.

### 콘텐츠 현황 (고도화 필요 이유)

시스템 골격은 갖춰졌으나, 기획서 MVP·정식 목표 대비 **데이터 콘텐츠가 부족**하다.

| 항목 | 현재(대략) | MVP 목표 | 정식 출시 목표 |
| --- | ---: | ---: | ---: |
| 직업 | 1 | 1 | 3 |
| 특성 | 1~4(샘플) | 4 | 15 |
| 일반 사건 | 2~3 | 20 | 80 |
| 엔딩(성공) | 5 | 6 | 기획 8종 + 확장 |
| 실패 엔딩 | 4 | 4 | 4 |
| 업적 | 소수 ID만 | 기본 세트 | 30 |
| 일일 미션 | 없음 | 제외 가능 | 20 |
| 배경·표정 | Placeholder | Placeholder 허용 | 배경 8 / 표정 6 |

### Rev.2에서 다룰 범위

1. **남은 시스템 단위 (원본 10~15)**: 메타 성장, 디버그, 광고 추상화, 분석, 실제 SDK, 출시 준비  
2. **콘텐츠·고도화 단위 (신규 16~)**: 사건/엔딩/특성 확충, 연쇄 사건, 아트·사운드, 일일 콘텐츠, 밸런스, UX

### 공통 기술 원칙 (원본과 동일)

* Unity 2D, C#, Portrait 1080×1920, Android 우선, 로컬 싱글플레이  
* 데이터와 로직 분리, UI가 GameState를 직접 수정하지 않음  
* 광고·분석은 인터페이스 추상화  
* 사건/직업/특성/엔딩은 ScriptableObject(또는 JSON)  
* 핵심 로직은 EditMode 테스트 가능  

### Rev.2 작업 시 주의

* 원본 Unit 10~15에 해당하는 **코드/에셋이 이미 일부 존재할 수 있다.**  
  → 새로 만들지 말고 **기존 구현을 먼저 분석·보완·검증**한다.  
* 한 요청에 여러 단위를 한꺼번에 구현하지 않는다.  
* 콘텐츠 단위는 **데이터 팩토리(Editor Menu) + SO 에셋 + 검증**을 기본 산출물로 한다.

---

# 개발 단위 10. 메타 성장과 도감

## 목표

반복 플레이 이유를 만드는 영구 성장 시스템을 완성한다.

## 구현 내용

* 인생 경험치 계산 (생존 일수, 남은 현금, 새 사건/엔딩, 업적)  
* 플레이어 레벨  
* 특성 해금 (`unlockLevel`)  
* 사건 도감 / 엔딩 도감  
* 업적 기본 세트  
* 해금 알림 이벤트  
* MainMenu 도감·해금률 UI 연결  

## 완료 기준

* 회차 종료 시 경험치가 MetaSaveData에 반영된다.  
* 레벨에 따라 특성이 해금된다.  
* 처음 본 사건·엔딩이 도감에 기록되고 중복 지급되지 않는다.  
* 메인 메뉴에서 도감·해금률을 확인할 수 있다.  

## Cursor 요청 프롬프트

```text
게임의 MetaProgression 시스템을 구현(또는 기존 구현을 보완)해줘.

기능:
- 인생 경험치
- 플레이어 레벨
- 특성 해금
- 사건 도감
- 엔딩 도감
- 업적

요구사항:
1. 기존 MetaProgressionManager / UnlockCodex / EndingCodex / MetaSaveData를 먼저 분석한다.
2. 회차 결과에 따라 경험치를 계산한다. 생존 일수, 남은 현금, 새 엔딩·사건 발견에 보너스를 준다.
3. TraitData.unlockLevel 기준으로 해금한다.
4. 중복 등록·중복 보상을 방지한다.
5. 메인 메뉴에서 해금률을 확인할 수 있게 한다.
6. 해금 시 알림 이벤트를 발생시킨다.
7. EditMode 테스트로 경험치·해금·중복 방지를 검증한다.
8. 다음 단위(디버그)까지 구현하지 마라.
```

---

# 개발 단위 11. 디버그 및 밸런스 도구

## 목표

사건·수치를 빠르게 검증하고, 자동 시뮬레이션으로 밸런스 지표를 뽑는다.

## 구현 내용

* DebugPanel: 날짜/능력치/시드/사건 강제/엔딩 강제  
* RunSimulator: N회 자동 플레이  
* 평균 생존 일수, 성공률, 실패 원인 비율, 평균 잔액 로그  
* Release 빌드에서 디버그 UI 비활성  

## 완료 기준

* 원하는 날짜·사건으로 즉시 이동할 수 있다.  
* 엔딩을 수동 테스트할 수 있다.  
* 1,000회 시뮬레이션 결과를 Console/파일로 확인할 수 있다.  
* Release에서는 디버그 메뉴가 비활성이다.  

## Cursor 요청 프롬프트

```text
DebugPanel과 RunSimulator를 구현(또는 보완)해줘.

DebugPanel:
- 날짜 변경, 현금·능력치 변경
- 특정 사건 강제 실행
- Random Seed 설정
- 특정 엔딩 강제 / 즉시 성공·실패

자동 시뮬레이터:
1. 지정 횟수만큼 30일 회차를 자동 실행한다.
2. 선택지는 랜덤 또는 정책(안전/절약/위험)으로 고른다.
3. 평균 생존 일수, 성공률, 실패 원인 비율, 평균 잔액을 출력한다.
4. Editor 전용으로 두고 Release Build에는 포함하지 않는다.
5. 기존 Debug/RunSimulator 코드를 중복 생성하지 마라.
```

---

# 개발 단위 12. 광고 시스템 추상화

## 목표

실제 SDK 없이 보상형·전면 광고 흐름을 게임 로직과 분리한다.

## 구현 내용

* `IAdService`, `MockAdService`  
* `RewardedAdPlacement` (리롤, 재시도, 긴급대출, 부업, 경험치 2배, 상점)  
* 회차/일 단위 횟수 제한  
* 광고 완료 후에만 보상 지급  
* UI 버튼 연결 (선택지 새로고침, 결과 화면 경험치 2배 등)  

## 완료 기준

* Editor에서 Mock 광고가 즉시 성공한다.  
* 실패 시 보상이 없다.  
* 같은 보상을 제한 초과로 반복 수령할 수 없다.  
* 게임 로직이 특정 SDK를 직접 호출하지 않는다.  

## Cursor 요청 프롬프트

```text
광고 SDK에 독립적인 광고 시스템을 구현(또는 보완)해줘.

요구사항:
1. IAdService: ShowRewardedAd, ShowInterstitial, IsRewardedReady
2. Editor용 MockAdService
3. RewardedAdPlacement Enum과 회차당/일당 쿼터
4. 완료 Callback 이후에만 AdRewardApplicator가 보상을 지급
5. 기획서 보상표:
   - 선택지 새로고침 회차 2회
   - 위험 선택 재시도 회차 1회
   - 긴급 대출 100,000원 회차 1회
   - 부업 30,000원 하루 1회
   - 경험치 2배 회차 1회
6. 광고 로딩 실패가 게임 진행을 막지 않게 한다.
7. 실제 AdMob은 연결하지 않는다.
8. 기존 Ads 폴더 코드를 먼저 검토한다.
```

---

# 개발 단위 13. 분석 이벤트

## 목표

이탈 지점과 선택 패턴을 측정할 수 있게 한다.

## 구현 내용

* `IAnalyticsService`, `DebugAnalyticsService`  
* 기획서 핵심 이벤트 (`game_start`, `run_started`, `event_shown`, `choice_selected`, `run_failed`, `run_completed`, 광고 관련 등)  
* 선택 파라미터(eventId, choiceIndex, day, cashBefore/After 등)  

## 완료 기준

* Editor Console에서 이벤트를 확인할 수 있다.  
* 사건 ID·선택지·실패 원인·생존 일이 기록된다.  
* 분석 코드가 게임 로직에 흩어지지 않는다.  

## Cursor 요청 프롬프트

```text
분석 이벤트 시스템을 구현(또는 보완)해줘.

요구사항:
1. IAnalyticsService + DebugAnalyticsService
2. 이벤트명은 snake_case
3. README 23절의 핵심 이벤트를 지원한다.
4. choice_selected에는 eventId, choiceIndex, day, cash/stat 전후값을 포함한다.
5. 게임 로직은 Firebase를 직접 알지 못하게 한다.
6. 개인 식별 정보를 기록하지 않는다.
7. 기존 Analytics 코드를 재사용한다.
```

---

# 개발 단위 14. 실제 광고 및 Firebase 연결

## 목표

MVP 플레이가 검증된 뒤 실제 수익화·분석 SDK를 붙인다.

## 시작 조건 (충족 전 착수 금지)

* 광고 없이도 재미가 있다.  
* MVP 사건 20개가 정상 동작한다. (**Unit 16 완료 권장**)  
* 저장·이어하기가 안정적이다.  
* 소규모 테스트 플레이 결과를 확보했다.  

## 구현 내용

* AdMob(또는 선택 SDK) 보상형·전면 광고  
* Firebase Analytics / Crashlytics  
* Remote Config로 전면 광고 빈도  
* 테스트 디바이스 설정  

## 완료 기준

* 테스트 광고가 출력된다.  
* 보상은 정확히 한 번만 지급된다.  
* 광고 실패 시에도 게임 진행 가능.  
* 원격 설정으로 전면 광고 빈도를 바꿀 수 있다.  

## Cursor 요청 프롬프트

```text
실제 광고·Firebase 연동을 진행해줘. 단, 시작 조건을 먼저 확인하고 미충족이면 구현하지 말고 체크리스트만 출력해라.

요구사항:
1. 기존 IAdService / IAnalyticsService / ICrashReporter / IRemoteConfigService를 구현체로 교체·연결한다.
2. Editor는 Mock, Device는 실제 SDK(또는 TestDevice 래퍼)를 쓰도록 SdkComposition을 정리한다.
3. 전면 광고 규칙(첫 3회차 미노출, 플레이 중 미노출, 결과→메인 이동 시, 2~3회차당 1회, 보상형 직후 미노출)을 지킨다.
4. Remote Config로 interstitial 빈도를 조절한다.
5. 테스트 방법을 단계별로 설명한다.
```

---

# 개발 단위 15. 출시 준비

## 목표

Google Play 내부 테스트용 Android App Bundle을 만든다.

## 구현 내용

* 앱 아이콘, 스플래시  
* 개인정보처리방침 링크  
* 광고 동의(Consent) 흐름  
* 사운드·진동 설정, 저장 초기화  
* 버전 관리, Release 빌드, 내부 테스트 배포  

## 완료 기준

* AAB 생성 성공  
* 실기기 30일 클리어 가능  
* 강제 종료 후 이어하기 가능  
* 오프라인 핵심 플레이 가능  
* 개인정보처리방침 화면 이동 가능  

## Cursor 요청 프롬프트

```text
출시 준비를 구현(또는 보완)해줘.

요구사항:
1. 스플래시, 설정(사운드/진동), 저장 초기화, 개인정보처리방침 열기
2. 광고 동의 패널 흐름
3. Android App Bundle 빌드에 필요한 Project Settings / Player Settings 체크리스트 제공
4. Release에서 DebugPanel 비활성 확인
5. 실기기 검증 시나리오(30일, 이어하기, 광고 실패)를 문서화한다.
```

---

# =========================
# 신규: 콘텐츠·고도화 단위
# =========================

---

# 개발 단위 16. MVP 사건 팩 20개

## 목표

기획서 §9 예시와 카테고리 균형을 맞춰 **플레이 가능한 사건 20개**를 채운다.

## 구현 내용

### 필수 포함 (기획 예시)

1. 갑작스러운 야근 (WORK)  
2. 친구의 결혼식 (RELATIONSHIP)  
3. 휴대전화 액정 파손 (ACCIDENT, 확률형)  
4. 점심 메뉴 (CONSUMPTION)  
5. 상사의 회식 제안 (WORK)  
6. 갑작스러운 감기 (HEALTH)  
7. 한정 할인 상품 (CONSUMPTION)  
8. 부업 제안 (OPPORTUNITY)  
9. 부모님 용돈 (RELATIONSHIP)  
10. 주식 관련 입문 사건 (OPPORTUNITY/SPECIAL) — 후속 연쇄용 flag 기록  

### 추가 권장 (10개)

* 월세/관리비/통신비/보험료 중 고정 지출 2~3개 (`FixedExpense`, 지정일 우선)  
* 카드 대금 (15일 전후)  
* 수면 부족, 허리 통증  
* 친구 약속 / 데이트 (주말 가중)  
* 중고거래(당근)  
* 지하철 지각·벌금  
* 집에서 쉬기 (REST, fallback 보완)  
* 월급 루팡·퇴사 충동 중 1 (SPECIAL, 후반부)  

### 데이터 규칙

* 선택지 3개: 안전 / 절약 / 위험(또는 확률) 성격 유지  
* `minDay`/`maxDay`/`weight`/`category`/`conditions` 필수  
* 주말 전용 또는 주말 가중 필드 활용  
* Editor Menu로 일괄 생성·검증  

## 완료 기준

* `Assets/Data/Events`에 유효한 사건 SO가 20개 이상이다.  
* 카테고리 비율이 기획 §11 권장에 크게 어긋나지 않는다.  
* EventSelector가 30일 동안 후보 고갈 없이(또는 fallback으로) 진행된다.  
* RunSimulator 100회에서 크래시 없이 완료된다.  

## Cursor 요청 프롬프트

```text
MVP용 사건 ScriptableObject 20개를 추가해줘.

요구사항:
1. README §7~§11, §9 예시를 따른다.
2. 기존 EventData / SampleDataFactory를 확장해 Editor Menu로 생성한다.
3. 각 사건은 선택지 3개, 고정 효과, 필요 시 RandomOutcome를 가진다.
4. FixedExpense는 지정 날짜에 우선 등장하도록 설정한다.
5. 주말용 인간관계·휴식 사건을 포함한다.
6. Validate All Data Assets가 경고 없이 통과하게 한다.
7. EventCatalog(또는 기존 등록 방식)에 모두 등록해 Game Scene에서 실제로 뽑히게 한다.
8. 시스템 리팩터는 최소화하고 데이터 추가에 집중한다.
```

---

# 개발 단위 17. MVP 엔딩·특성·직업 데이터 완성

## 목표

MVP 범위의 **엔딩 6종(성공) + 실패 4종**, **특성 4종**, 직업 1종의 데이터를 완비한다.

## 구현 내용

### 성공 엔딩 (우선순위 포함)

| 엔딩 | 조건(기획) |
| --- | --- |
| 통장 잔고의 제왕 | 현금 1,000,000 이상 |
| 건강한 직장인 | 건강 70↑, 스트레스 40↓ |
| 승진 후보 | 회사 평가 80↑ |
| 행복한 소비왕 | 행복도 80↑ |
| 겨우 살아남았다 | 생존 fallback |
| (추가 1) 카드 돌려막기 / 퇴사 준비 / 인생은 한방 중 MVP에 맞는 1~2 | 조건 정의 |

실패 엔딩: 파산, 번아웃, 입원, 해고 — 기존 유지·문구 검수.

### 특성 4종 (MVP)

* 짠돌이, 체력왕, 긍정왕, 야근전문가 (또는 눈치왕)  
* `unlockLevel` 설정  
* **효과가 실제로 적용**되도록 TraitEffect 또는 수정자 연결 (Unit 18과 연계 가능)

## 완료 기준

* EndingEvaluator가 동시 충족 시 우선순위로 하나만 고른다.  
* Result Scene에 제목·설명이 표시된다.  
* 특성 4개가 도감/해금과 연결된다.  

## Cursor 요청 프롬프트

```text
MVP 엔딩·특성 데이터를 완성해줘.

요구사항:
1. README §12~§15, §21을 기준으로 EndingData / TraitData SO를 채운다.
2. 성공 엔딩 최소 6개, 실패 4개가 동작한다.
3. 우선순위 충돌 테스트를 작성·보강한다.
4. 특성 4개에 unlockLevel과 효과 정의 필드를 명확히 한다.
5. 기존 SampleEndingFactory / SampleDataFactory를 재사용한다.
6. UI 카피는 한국어로 작성한다.
```

---

# 개발 단위 18. 회차 시작 플로우 (직업·특성 선택)

## 목표

메인 메뉴 → 직업 확인 → 특성 선택 → 회차 시작이 기획 루프와 맞게 동작한다.

## 구현 내용

* 직업 정보 표시 (MVP는 1개 고정이어도 UI는 확장 가능하게)  
* 해금된 특성만 선택 가능  
* 선택 특성을 GameState 초기화에 반영  
* Analytics: `job_selected`, `trait_selected`, `run_started`  
* 이어하기와 신규 시작 분기 명확화  

## 완료 기준

* 특성 없이 시작하거나, 해금 특성 선택 후 시작이 가능하다.  
* 짠돌이 등 효과가 첫날부터 수치/비용에 반영된다.  
* 저장 후 재시작해도 선택 특성이 유지된다.  

## Cursor 요청 프롬프트

```text
회차 시작 플로우(직업·특성 선택)를 구현해줘.

요구사항:
1. MainMenu에서 새 게임 시 특성 선택 UI를 제공한다.
2. MetaProgression에서 해금된 특성만 선택 가능하다.
3. 선택한 Trait의 효과가 GameState 초기화 또는 Effect 적용 파이프라인에 반영된다.
   - 짠돌이: 생활비성 현금 감소 5% 완화(또는 기획 수치), 행복 획득 감소
   - 체력왕: 건강 상한/시작 보너스
   - 야근전문가: WORK 야근 스트레스 감소
4. UI는 GameState를 직접 수정하지 않는다.
5. 저장 데이터에 선택 특성 ID를 포함한다.
6. 테스트로 특성 적용 전후 수치를 검증한다.
```

---

# 개발 단위 19. 주간 결산·고정비·난도 연출

## 목표

날짜 시스템의 “리듬”을 플레이어가 체감하게 한다.

## 구현 내용

* 7/14/21일 주간 결산 팝업 (능력치 요약, 경고)  
* 15일 카드/고정비 이벤트 강제 또는 우선  
* 28~29일 위기 연출 (UI 경고색, 카피)  
* 30일 월급 입금 연출  
* 난도 계수에 따른 효과 스케일(선택: cash 손실만 스케일)  

## 완료 기준

* 주간 결산일이 스킵되지 않는다.  
* 후반부로 갈수록 평균 손실이 커지는 것을 시뮬레이터로 확인할 수 있다.  
* 회복 사건 비율이 완전히 사라지지 않는다.  

## Cursor 요청 프롬프트

```text
주간 결산·고정비·후반 위기 연출을 구현해줘.

요구사항:
1. DayManager의 WeeklySummary를 UI 팝업으로 연결한다.
2. 15일 전후 FixedExpense(카드/고정비)가 우선 등장한다.
3. 28~29일에는 위기 상태를 HUD/연출로 강조한다.
4. Day 30 성공 시 월급 입금 연출 후 Result로 이동한다.
5. 난도 계수 적용 지점(EffectResolver 또는 Selector weight)을 명확히 문서화한다.
6. 기존 날짜 로직을 깨지 않도록 테스트를 보강한다.
```

---

# 개발 단위 20. 연쇄·조건부 사건

## 목표

“이전 선택이 다음 사건을 만든다”는 긴장감을 구현한다.

## 구현 내용

* RunFlag / RunHistory 기반 조건 (`hasBoughtStock`, `owesDebt`, `phoneStillCracked` 등)  
* 주식 매수 → 주식 급락/급등 후속 사건  
* 사설 수리 실패 → 재고장 사건  
* 빚/긴급대출 사용 시 “카드 돌려막기” 엔딩 조건 연결  
* EventCondition 확장  

## 완료 기준

* 플래그 없이 후속 사건이 나오지 않는다.  
* 시드 고정 시 연쇄가 재현된다.  
* 엔딩 “인생은 한방”, “카드 돌려막기”가 플래그와 연동된다.  

## Cursor 요청 프롬프트

```text
연쇄·조건부 사건 시스템을 구현해줘.

요구사항:
1. Run 단위 플래그(또는 태그)를 GameState/RunHistory에 저장한다.
2. EventCondition에서 플래그 요구/금지를 지원한다.
3. 기획 §9.10 주식 급락처럼 선행 사건 이후에만 등장하는 사건을 2개 이상 추가한다.
4. 확률형 실패가 후속 사건을 큐잉할 수 있게 한다(과도한 복잡도는 피한다).
5. 저장·이어하기 시 플래그가 유지된다.
6. EditMode 테스트로 조건 필터를 검증한다.
```

---

# 개발 단위 21. 아트 파이프라인 (배경·표정)

## 목표

Placeholder를 유지하되, 실에셋 교체가 쉬운 구조를 만든다. (에셋 자체는 단계적)

## 구현 내용

* 배경 슬롯: 집/회사/지하철/식당/병원 (+예비 3)  
* 캐릭터 표정 슬롯: 기본/행복/당황/분노/피곤/절망  
* EventData → 배경·표정 매핑  
* 선택 결과 후 표정 전환  
* Addressable 또는 Resources 로딩 규약 문서화  

## 완료 기준

* 사건 카테고리별로 다른 배경 Placeholder가 표시된다.  
* 능력치 변화에 따라 표정이 바뀐다.  
* 이미지 파일이 없어도 NPE 없이 동작한다.  

## Cursor 요청 프롬프트

```text
아트 슬롯·매핑 파이프라인을 구현해줘.

요구사항:
1. 배경 5종, 표정 6종 슬롯을 ScriptableObject 또는 Catalog로 관리한다.
2. EventData에 backgroundId, expressionId(또는 category 기본값)를 연결한다.
3. Game UI는 키로 스프라이트를 찾아 표시하고, 없으면 Placeholder를 쓴다.
4. 실에셋 교체 방법(폴더 경로, 네이밍)을 README 또는 Docs에 짧게 적는다.
5. 무거운 애니메이션 대신 표정 교체 + 약한 shake만 허용한다.
```

---

# 개발 단위 22. 사운드·BGM

## 목표

기획 §20의 최소 사운드 세트를 붙인다.

## 구현 내용

* SFX: 클릭, 현금±, 스트레스↑, 성공/실패 징글, 월급 입금  
* BGM: 메인 / 플레이 / 위기 / 결과 4종  
* 설정 mid의 ON/OFF·볼륨과 연동  
* AudioService 추상화  

## 완료 기준

* 설정에서 사운드를 끄면 모든 오디오가 멈춘다.  
* 위기(스트레스 높음/28일+)에서 BGM이 전환된다.  
* 오디오 클립이 없어도 예외 없이 동작한다.  

## Cursor 요청 프롬프트

```text
사운드·BGM 시스템을 구현해줘.

요구사항:
1. IAudioService + 설정 연동
2. BGM 4상태, SFX 최소 세트
3. 이벤트 훅: 선택, 현금 변화, 실패/성공, 월급
4. 클립 미할당 시 no-op
5. Placeholder 오디오(무음/짧은 beep)로도 파이프라인이 동작하게 한다.
```

---

# 개발 단위 23. 정식 콘텐츠 확장 1차 (직업·사건)

## 목표

정식 출시 최소 콘텐츠의 **1차 배치**를 채운다. (전부 한 번에 80개 금지)

## 구현 내용

* 직업 +2 (예: 공무원 준비생, 프리랜서 또는 대기업 사원)  
* 직업별 전용 사건 각 8~15개 중 **1차 8개씩**  
* 일반 사건 추가: 누적 40개 목표 (MVP 20 + 20)  
* 직업별 월급·고정비·시작 능력치 차별화  

## 완료 기준

* 직업 3개 중 해금/선택 가능.  
* 직업 전용 사건이 다른 직업에 나오지 않는다.  
* 시뮬레이터로 직업별 성공률 차이를 확인한다.  

## Cursor 요청 프롬프트

```text
정식 콘텐츠 1차(직업 확장 + 사건 추가)를 구현해줘.

요구사항:
1. README §14 직업 확장표에서 MVP 외 2개 직업을 추가한다.
2. JobData 조건으로 전용 사건을 필터한다.
3. 일반 사건 풀을 40개 수준까지 늘린다(기존 20 + 신규 20).
4. 밸런스는 RunSimulator로 직업별 성공률을 로그한다.
5. 메타 해금(레벨/경험치)으로 새 직업을 연다.
6. 한 번에 80개 전체는 만들지 마라.
```

---

# 개발 단위 24. 엔딩·업적·도감 고도화

## 목표

기획 성공 엔딩 풀과 업적 30개 중 핵심을 채운다.

## 구현 내용

* 성공 엔딩 추가: 카드 돌려막기, 퇴사 준비 완료, 인생은 한방 등  
* 업적 15~30개 정의 및 트리거  
* 도감 UI: 미해금 실루엣, 해금률, 필터  
* 결과 화면에서 신규 해금 하이라이트  

## 완료 기준

* 엔딩 도감에 기획 주요 엔딩이 등록된다.  
* 업적 달성 시 메타 보상(경험치/조각)이 지급된다.  
* 미해금 항목이 스포일러 없이 표시된다.  

## Cursor 요청 프롬프트

```text
엔딩·업적·도감 고도화를 구현해줘.

요구사항:
1. README §13 엔딩표를 최대한 데이터로 반영한다.
2. 업적 목록을 AchievementIds/SO로 확장하고 트리거를 MetaProgression에 연결한다.
3. CodexPanel에서 엔딩/사건/업적 탭을 제공한다.
4. 신규 해금 시 결과·메뉴 알림을 띄운다.
5. 기존 저장 version 마이그레이션이 필요하면 SaveData version을 올린다.
```

---

# 개발 단위 25. 일일 콘텐츠 (오늘의 직장인·일일 미션)

## 목표

짧은 재방문 루프를 만든다. (온라인 랭킹은 제외, 로컬만)

## 구현 내용

* 날짜 시드 기반 “오늘의 직장인” 고정 시드 회차  
* 로컬 베스트 스코어 저장 (잔액, 생존, 스트레스 등)  
* 일일 미션 5~10개 풀 + 매일 1~3개 배정  
* 보상: 인생 경험치 / 특성 조각  

## 완료 기준

* 같은 캘린더 날짜에는 같은 시드로 사건이 재현된다.  
* 미션 진행도가 저장된다.  
* 자정(로컬) 기준 갱신이 동작한다.  

## Cursor 요청 프롬프트

```text
일일 콘텐츠(오늘의 직장인 + 일일 미션)를 로컬 전용으로 구현해줘.

요구사항:
1. README §17을 따른다. 온라인 랭킹은 만들지 않는다.
2. 날짜 문자열로 시드를 만들어 EventSelector에 주입한다.
3. 로컬 베스트만 MetaSaveData에 저장한다.
4. 일일 미션 예시(배달 없이 10일, 회사평가 80, 잔액 50만 등)를 데이터화한다.
5. 메인 메뉴에 진입점을 추가한다.
```

---

# 개발 단위 26. UX·튜토리얼·카피 폴리시

## 목표

첫 회차 이탈을 줄인다. (KPI: 튜토리얼 완료율, 7일 도달률)

## 구현 내용

* 첫 회차 3~5스텝 튜토리얼 (능력치 의미, 선택 트레이드오프)  
* 위기/파산 직전 안내 카피  
* 결과 화면 “다음엔 이렇게” 팁  
* 버튼 피드백, 빈 상태(도감/이어하기 없음) 처리  
* 한국어 문장 검수  

## 완료 기준

* 첫 실행 시 튜토리얼이 1회 표시되고 스킵 가능하다.  
* 능력치 툴팁 또는 짧은 설명이 있다.  
* 치명적 UI 막힘(입력 불가)이 없다.  

## Cursor 요청 프롬프트

```text
첫 회차 UX·튜토리얼·카피 폴리시를 구현해줘.

요구사항:
1. MetaSave의 firstRun 플래그로 튜토리얼 노출을 제어한다.
2. 능력치 5종의 짧은 설명을 제공한다.
3. 파산/번아웃 직전 경고 카피를 넣는다.
4. 결과 화면 팁은 실패 원인별로 다르게 한다.
5. 기존 UI 구조를 유지하고 과도한 연출은 피한다.
```

---

# 개발 단위 27. 밸런스 패스

## 목표

시뮬레이터와 소규모 플레이로 난도를 MVP 목표 KPI에 가깝게 맞춘다.

## 구현 내용

* RunSimulator 정책별(안전/절약/위험/랜덤) 리포트  
* 날짜 구간별 평균 잔액·실패율  
* weight·효과 수치 조정 패치  
* “강제 실패”가 되지 않도록 회복 사건 비율 점검  

## 목표 지표 (초기 검증)

* 랜덤 정책 기준 첫 회차 7일 도달 ≈ 70%  
* 15일 도달 ≈ 50%  
* 30일 성공은 너무 쉽지도, 불가능에 가깝지도 않게  

## 완료 기준

* 조정 전후 시뮬 리포트를 남긴다.  
* 극단적 한 방 실패(1일차 파산)가 과도하지 않다.  
* 후반 위기감은 유지된다.  

## Cursor 요청 프롬프트

```text
밸런스 패스를 진행해줘.

요구사항:
1. RunSimulator로 1000회 × 정책별 리포트를 뽑는 Editor 도구/메뉴를 보강한다.
2. 실패 원인 비율과 일자별 생존 곡선을 출력한다.
3. EventData weight/effects만 조정하고 시스템 리팩터는 하지 마라.
4. 조정 근거를 짧게 요약해 Docs/BalanceNotes.md 또는 Console 요약으로 남긴다.
5. README KPI를 목표로 하되, 수치를 억지로 맞추기 위해 선택지를 무의미하게 만들지 마라.
```

---

# 개발 단위 28. 광고 제거 상품(선택) · 상점 UI

## 목표

전면 광고만 제거하는 상품 구조를 준비한다. (실제 IAP는 플래그로 Mock 가능)

## 구현 내용

* `IPurchaseService` + Mock  
* 광고 제거 소유 여부 MetaSave 저장  
* 전면 광고 게이트에서 소유 시 스킵  
* 보상형 광고는 유지  
* 무료 상점(광고로 특성 조각) UI  

## 완료 기준

* Mock 구매 후 전면 광고가 나오지 않는다.  
* 보상형 광고는 계속 선택 가능하다.  
* 구매 정보가 로컬에만 저장된다(민감 결제 정보 없음).  

## Cursor 요청 프롬프트

```text
광고 제거·무료 상점 구조를 구현해줘.

요구사항:
1. README §16 광고 제거 상품 규칙을 따른다(전면만 제거).
2. IPurchaseService Mock으로 구매를 시뮬레이션한다.
3. Interstitial 노출 조건에 NoAds 플래그를 반영한다.
4. 실제 스토어 IAP SDK는 아직 연결하지 않아도 된다.
5. 무료 상점: 보상형 광고로 특성 조각, 하루 3회.
```

---

# 권장 개발 순서 (Rev.2)

```text
[시스템 마무리]
10 메타 성장·도감
11 디버그·시뮬레이터
12 광고 추상화 + UI 연결
13 분석 이벤트

[콘텐츠로 MVP 완성]
16 MVP 사건 20개     ← 재미 검증의 핵심
17 MVP 엔딩·특성
18 회차 시작(특성 선택·효과)
19 주간 결산·고정비·위기 연출
27 밸런스 패스(1차)

[알파]
20 연쇄 사건
21 아트 파이프라인
22 사운드
26 UX·튜토리얼

[수익화·출시]
14 실제 SDK (시작 조건 충족 후)
15 출시 준비

[정식 확장]
23 직업·사건 확장 1차
24 엔딩·업적 고도화
25 일일 콘텐츠
28 광고 제거·상점
27 밸런스 패스(2차)
```

---

# Cursor 작업 규칙 (공통)

각 단위 요청 시 아래를 붙인다.

```text
공통 규칙:
1. 기존 코드를 먼저 분석하고 중복 클래스를 만들지 마라.
2. Cursor-WBS.md Unit 1~9 결과물과 Rev.2 단위가 충돌하면 기존 설계를 우선하고 최소 수정으로 확장하라.
3. UI와 게임 로직을 분리하라.
4. SerializeField private를 우선 사용하라.
5. null 검사를 하고 오류 메시지를 명확히 하라.
6. 수정한 파일 목록을 마지막에 정리하라.
7. Unity Editor 수동 작업을 단계별로 설명하라.
8. 구현 후 테스트 방법을 제공하라.
9. 파괴적 변경 가능성이 있으면 영향 범위를 먼저 설명하라.
10. 한 번에 다음 개발 단위까지 구현하지 마라.
11. 커밋 메시지가 필요하면 한글(용어 제외)로 제안하라.
```

---

# 개발 완료 체크포인트 (Rev.2)

## 알파 (광고 없이 재미있는 MVP)

* Unit 10~11, 16~19, 27(1차)  
* 사건 20+, 특성 4, 엔딩 6+, 특성 선택, 주간 결산  
* 시뮬레이터로 난도 윤곽 확인  

## 베타 (폴리시)

* Unit 20~22, 26  
* 연쇄 사건, 아트/사운드 파이프라인, 튜토리얼  

## 수익화 테스트

* Unit 12~14, 28(선택)  
* Mock→실기기 광고, 분석 이벤트  

## 출시 후보

* Unit 15  
* AAB, 동의, 방침, 실기기 30일·이어하기  

## 정식 콘텐츠

* Unit 23~25, 27(2차)  
* 직업 3, 사건 40+(점진적 80), 일일 콘텐츠, 업적·엔딩 확장  

---

# 단위별 한 줄 요청 템플릿

```text
Cursor-WBS-Rev2.md의 개발 단위 N을 구현해줘.
공통 규칙을 지키고, 기존 코드를 먼저 분석한 뒤 해당 단위만 완료 기준까지 마무리해줘.
```
